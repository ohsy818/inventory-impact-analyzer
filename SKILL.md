---
name: inventory-impact-analyzer
description: 입고 지연 건이 재고와 생산에 미치는 영향을 분석한다. 현재 재고와 일평균 사용량으로 재고 소진 예상일을 계산하고, 변경 입고 예정일과 비교해 생산 차질 가능성과 심각도를 판단한다. WMS 입고 지연 대응 프로세스에서 재고 분석 에이전트가 "입고 지연 건 등록" 직후 "재고 및 생산 영향 분석" 태스크를 수행할 때 반드시 이 스킬을 사용한다. 재고 소진일, 소진 예상일, 생산 차질 가능성, 안전재고, 입고 지연에 따른 재고 영향, 여유일수 같은 말이 나오면 사용자가 "스킬"을 명시하지 않아도 이 스킬을 사용해라.
---

# 재고 및 생산 영향 분석

입고가 지연된 건이 재고 고갈과 생산 차질로 이어질지를 숫자로 판단해, 다음 단계(긴급 대응 필요 여부 판단)가 근거를 갖고 진행되도록 하는 것이 이 스킬의 목적이다. 추측이 아니라 재고 소진일이라는 구체적인 날짜로 답한다.

## 필요한 입력값

- 품목
- 현재 재고 수량
- 일평균 사용량(생산에서 소비하는 양) — 직접 주어지지 않으면 최근 기간의 평균 소비량으로 추정하되, 추정치임을 반드시 밝힌다
- 기존 입고 예정일, 변경 입고 예정일
- (선택) 안전재고 수준 — 없으면 0으로 간주하고 그 사실을 명시한다
- (선택) 리드타임

값이 비어 있으면 임의로 채우지 말고, odoo-mcp 혹은 wms-mcp 를 사용하여 직접 값을 조회한다.

## 계산 로직

1. 가용재고 = 현재 재고 수량 − 안전재고 수준
2. 재고 소진 예상일 = 오늘 날짜 + floor(가용재고 ÷ 일평균 사용량)
   - 일평균 사용량이 0이거나 없으면 소진일을 계산할 수 없다는 것을 그대로 보고한다 (억지로 숫자를 만들지 않는다)
3. 여유일수 = 재고 소진 예상일 − 변경 입고 예정일
   - 0 이상: 재고가 변경 입고일까지 버틴다
   - 음수: 변경 입고일 이전에 재고가 먼저 바닥난다 → 생산 차질 가능성

계산은 반복적이고 실수하기 쉬우므로 `scripts/depletion_calc.py`를 사용한다:

```
python3 scripts/depletion_calc.py --current-stock 500 --daily-usage 40 --safety-stock 50 --changed-eta 2026-08-10
```

스크립트를 쓸 수 없는 환경이면 동일한 산식을 직접 계산해도 된다.

## 심각도 판단 기준 (기본값)

| 여유일수 | 생산 차질 가능성 | 심각도 |
|---|---|---|
| 3일 이상 | 낮음 | 안전 |
| 0~2일 | 있음 | 주의 |
| 음수 | 있음 (이미 공백 발생 예상) | 위험 |

이 구간은 업종 표준이 아니라 상식적인 기본값이다. 리드타임이 유난히 길거나 대체 공급이 어려운 품목이면 더 보수적인 기준(예: 5일 이상을 "안전"으로)을 적용하는 것이 합리적이며, 조직이 별도 기준을 갖고 있다면 그것을 우선한다.

## 출력 형식

다음 단계(입고 지연 대응 에이전트의 긴급 대응 판단)가 그대로 이어받아 쓸 수 있도록 항상 이 형식으로 요약한다.

**재고 및 생산 영향 분석 결과**
- 품목 / PO: ...
- 현재 재고: ... / 일평균 사용량: ...
- 재고 소진 예상일: YYYY-MM-DD
- 변경 입고 예정일: YYYY-MM-DD
- 여유일수: N일
- 생산 차질 가능성: 있음 / 낮음
- 심각도: 안전 / 주의 / 위험
- 판단 근거: (한두 문장, 가정을 썼다면 명시)
